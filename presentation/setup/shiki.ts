import { defineShikiSetup } from "@slidev/types";

export default defineShikiSetup(() => {
  return {
    themes: {
      dark: "one-dark-pro",
      light: "min-light",
    },
    transformers: [
      // ...
    ],
  };
});
